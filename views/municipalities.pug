extends layout

block content

        br
        br
        div.container.mt-5
            h2.text-center Listado de todos los municipios madrileños con holandeses
            -if(data.length)
                table.table
                    thead
                        tr
                            th Municipio
                            th Usuarios Registrados
                            th Enviar mensaje al grupo
                    each x in data     
                        tr
                            td.municipality=x._id
                            td.users=x.total
                            td
                                a(href=`/municipalities/messages/${x._id}`) Mensaje
        #map-template
        script(src='/dist/leaflet/leaflet.js')
        script(src='/js/script.js')

    

        